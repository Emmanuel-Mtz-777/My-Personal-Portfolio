---
import es from "../../langs/es.json";
import en from "../../langs/en.json";

import ExperienceCard from "../ui/cards/ExperienceCard.astro";

const { lang = "es" } = Astro.props;

const t = lang === "es" ? es : en;

const experiences = t.experience.items || [];
---

<section
  id="experience"
  class="relative w-full flex flex-col items-center pt-20 px-4"
>
  <h2 class="text-2xl sm:text-4xl font-bold text-white mb-12">
    {t.experience.title}
  </h2>
  <div class="w-full flex flex-col gap-6 items-center rounded-lg p-6">
    {experiences.map((exp) => (
      <ExperienceCard exp={exp} />
    ))}
  </div>
</section>





